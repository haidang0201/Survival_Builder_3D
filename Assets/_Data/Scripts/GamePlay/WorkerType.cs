public enum WorkerType
{
    WorkStation,      // Công trình làm việc (ví dụ: nhà máy)
    Home,             // Nhà ở của công nhân
    ResourceGathering, // Thu thập tài nguyên (ví dụ: chặt cây, khai thác đá)
    // Các loại công việc khác nếu cần
}